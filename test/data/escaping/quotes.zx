pub fn Page(allocator: zx.Allocator) zx.Component {
    return (
        <div @allocator={allocator}>
                quote should be escaped 
            <code>"quote"</code>
            <pre>"quote"</pre>
        </div>
    );
}

const zx = @import("zx");
